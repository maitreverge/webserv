#include <iostream>
#include <string>
#include <string>
#include <sys/stat.h>

using namespace std;


int main(void)
{
	string saveURI = "directoryONE/directoryTwo/helohelohelo.html";

	string index = "helohelohelo.html";

	cout << "Original String = " << saveURI << endl;

	saveURI.erase(saveURI.find_last_of("/") + 1, index.size());

	std::cout << "Modified URI = " << saveURI << std::endl;
	
}